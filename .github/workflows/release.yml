name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    uses: MasonRhodesDev/packaging-workflows/.github/workflows/release.yml@a1b85c42addd5e876f301886f5c6ea9d1cc9b168
    secrets: inherit
    with:
      spec-name: hyprstate
      security: true
